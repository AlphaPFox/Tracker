<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_filter"
        app:showAsAction="ifRoom"
        android:title="@string/action_filter"
        android:icon="@drawable/ic_filter_list_black_24dp"/>

    <item android:id="@+id/action_edit"
        app:showAsAction="never"
        android:title="@string/action_edit"
        android:icon="@drawable/ic_mode_edit_black_24dp"/>

    <item android:id="@+id/action_request_position"
        app:showAsAction="never"
        android:visible="false"
        android:title="Solicitar posição atual" />

    <item android:id="@+id/action_request_status"
        app:showAsAction="never"
        android:visible="false"
        android:title="Solicitar status (Bat./GSM)" />

    <item android:id="@+id/action_turn_off"
        app:showAsAction="never"
        android:visible="false"
        android:title="Desligar temporariamente" />

    <item android:id="@+id/action_reset_config"
        app:showAsAction="never"
        android:visible="true"
        android:title="Reiniciar configurações" />

</menu>